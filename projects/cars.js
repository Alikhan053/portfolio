'use strict';
const vehicles=[{id:1,name:'Apex GT3',make:'Aurora',type:'Sports',power:502,price:165000,drive:'RWD',color:20},{id:2,name:'M4 Competition',make:'Bavarian Concept',type:'Sports',power:503,price:85000,drive:'AWD',color:100},{id:3,name:'Terrain G',make:'Meridian',type:'SUV',power:416,price:140000,drive:'AWD',color:0},{id:4,name:'RS Touring',make:'Atlas',type:'Touring',power:591,price:125000,drive:'AWD',color:160},{id:5,name:'Trail 110',make:'Rover Concept',type:'SUV',power:296,price:60000,drive:'AWD',color:40},{id:6,name:'Grand Panora',make:'Aurora',type:'Touring',power:325,price:98000,drive:'RWD',color:200},{id:7,name:'Volt S',make:'Electron',type:'Electric',power:670,price:82000,drive:'AWD',color:260},{id:8,name:'Ionic GT',make:'Atlas',type:'Electric',power:523,price:105000,drive:'AWD',color:300},{id:9,name:'Sprint R',make:'Meridian',type:'Sports',power:443,price:75000,drive:'RWD',color:340}];
const $=s=>document.querySelector(s),storageKey='mohid-car-favorites-v1';let favorites=new Set(),compare=new Set(),storageOK=true;
try{const saved=JSON.parse(localStorage.getItem(storageKey));if(Array.isArray(saved))favorites=new Set(saved.filter(id=>vehicles.some(c=>c.id===id)));}catch{storageOK=false;}
function render(){const q=$('#search').value.trim().toLowerCase(),type=$('#category').value;let list=vehicles.filter(c=>(c.name+' '+c.make).toLowerCase().includes(q)&&(type==='all'||type===c.type)&&(!$('#favorites-only').checked||favorites.has(c.id)));list.sort($('#sort').value==='power'?(a,b)=>b.power-a.power:$('#sort').value==='price'?(a,b)=>a.price-b.price:(a,b)=>a.name.localeCompare(b.name));$('#status').textContent=`${list.length} vehicles · ${favorites.size} favorites · ${compare.size}/3 selected for comparison${storageOK?'':' · Storage unavailable: favorites last for this visit only'}`;$('#catalog').innerHTML=list.length?list.map(c=>`<article class="auto-card"><div class="auto-illustration" style="filter:hue-rotate(${c.color}deg)" aria-hidden="true"><div class="car-art"><div class="car-window"></div><div class="car-body"></div><i class="wheel wheel-one"></i><i class="wheel wheel-two"></i><div class="headlight"></div></div></div><div class="auto-content"><span class="eyebrow">${c.make} / ${c.type}</span><h2>${c.name}</h2><p>${c.power} hp · ${c.drive}<br>Illustrative price: $${c.price.toLocaleString('en-US')}</p><button class="small-button" data-favorite="${c.id}" aria-pressed="${favorites.has(c.id)}" aria-label="Favorite ${c.name}">${favorites.has(c.id)?'♥ Saved':'♡ Favorite'}</button><button class="small-button" data-compare="${c.id}" aria-pressed="${compare.has(c.id)}" aria-label="Compare ${c.name}">${compare.has(c.id)?'✓ Selected':'+ Compare'}</button></div></article>`).join(''):'<div class="empty">No vehicles match. Try another search or turn off favorites only.</div>';renderComparison();const favoriteTypes=new Set(vehicles.filter(c=>favorites.has(c.id)).map(c=>c.type));const recommendations=vehicles.filter(c=>favoriteTypes.has(c.type)&&!favorites.has(c.id)).slice(0,3);$('#recommendations').hidden=!recommendations.length;$('#recommendation-text').textContent='Based on your favorite body styles: '+recommendations.map(c=>c.name+' ('+c.type+')').join(' · ')+'. Search these names above to explore.';}
function renderComparison(){const selected=vehicles.filter(c=>compare.has(c.id));$('#comparison').hidden=!selected.length;$('#comparison-table').innerHTML='<thead><tr><th>Specification</th>'+selected.map(c=>'<th>'+c.name+'</th>').join('')+'</tr></thead><tbody>'+[['Body style','type'],['Power (hp)','power'],['Drive','drive'],['Sample price (USD)','price']].map(([label,key])=>'<tr><td>'+label+'</td>'+selected.map(c=>'<td>'+c[key]+'</td>').join('')+'</tr>').join('')+'</tbody>';}
$('#catalog').addEventListener('click',e=>{const fav=e.target.closest('[data-favorite]'),comparison=e.target.closest('[data-compare]');if(fav){const id=Number(fav.dataset.favorite);favorites.has(id)?favorites.delete(id):favorites.add(id);try{localStorage.setItem(storageKey,JSON.stringify([...favorites]));}catch{storageOK=false;}render();}if(comparison){const id=Number(comparison.dataset.compare);if(compare.has(id))compare.delete(id);else if(compare.size<3)compare.add(id);else{$('#status').textContent='Compare up to three vehicles. Deselect one to add another.';return;}render();}});
$('#search').addEventListener('input',render);for(const id of ['category','sort','favorites-only'])$('#'+id).addEventListener('change',render);$('#clear-comparison').addEventListener('click',()=>{compare.clear();render();});render();
